version: '3.1'

services:
  database:
    container_name: postgres-parking-control
    image: postgres:13
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: parking-control
